# Test fixtures

Everything in this directory is **synthetic** and is named with a `synthetic_` prefix. Values
are deliberately artificial (round numbers, obvious patterns) so nothing here could be
mistaken for real observations. Never copy fixtures under any `projects/*/data/` directory.
